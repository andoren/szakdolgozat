<UserControl x:Class="IktatogRPCClient.Views.TorzsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IktatogRPCClient.Views"
             mc:Ignorable="d" 
             FontSize="18"
             FontFamily="Roboto"
             d:DesignHeight="600" d:DesignWidth="1100">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock FontSize="24" Margin="5">Felhasználói beállítások:</TextBlock>
             <WrapPanel Margin="5 0">
                <ContentControl x:Name="UgyintezokViewModel"/>
                <ContentControl x:Name="JellegViewModel"/>
                <ContentControl x:Name="PartnerViewModel"/>
            </WrapPanel>
            <StackPanel x:Name="AdminSettingsIsVisible">
                <TextBlock FontSize="24" Margin="5">Adminisztrátori beállítások:</TextBlock>
                <WrapPanel Margin="5 0">
                    <ContentControl x:Name="TelephelyViewModel"/>
                    <ContentControl x:Name="FelhasználóViewModel"/>
                    <ContentControl x:Name="CsoportViewModel"/>
                </WrapPanel>
            </StackPanel>
  
        </StackPanel>
        
    </Grid>
</UserControl>
