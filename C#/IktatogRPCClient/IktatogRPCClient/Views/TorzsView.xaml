<UserControl x:Class="IktatogRPCClient.Views.TorzsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IktatogRPCClient.Views"
             mc:Ignorable="d" 
             FontSize="18"
             FontFamily="Roboto"
             d:DesignHeight="600" d:DesignWidth="1100">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <WrapPanel   HorizontalAlignment="Center">
                <ContentControl  Margin="10" x:Name="UgyintezokViewModel"/>
                <ContentControl  Margin="10"  x:Name="JellegekViewModel"/>
                <ContentControl  Margin="10" x:Name="PartnersViewModel"/>
                <ContentControl  Margin="10" x:Name="PartnerekUgyintezoiViewModel"/>
                <ContentControl IsEnabled="{Binding Path=AdminSettingsIsVisible}" Margin="10" x:Name="TelephelyekViewModel" />
                <ContentControl IsEnabled="{Binding Path=AdminSettingsIsVisible}" Margin="10" x:Name="UsersViewModel"/>
                <ContentControl IsEnabled="{Binding Path=AdminSettingsIsVisible}" Margin="10" x:Name="CsoportokViewModel"/>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
