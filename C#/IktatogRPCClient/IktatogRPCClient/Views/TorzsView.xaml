<UserControl x:Class="IktatogRPCClient.Views.TorzsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IktatogRPCClient.Views"
             mc:Ignorable="d" 
             FontSize="18"
             FontFamily="Roboto"
             d:DesignHeight="600" d:DesignWidth="1100">
    <Grid >
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                
                </Grid.ColumnDefinitions>
                <ContentControl Grid.Column="0" Margin="10" x:Name="UgyintezokViewModel"/>
                <ContentControl Grid.Column="1"  Margin="10"  x:Name="JellegekViewModel"/>
                <ContentControl Grid.Column="2"  Margin="10" x:Name="PartnersViewModel"/>
                <ContentControl Grid.Row="1" Grid.Column="0"  Margin="10" x:Name="PartnerekUgyintezoiViewModel"/>
                <ContentControl Grid.Row="1" Grid.Column="1"  Margin="10" IsEnabled="{Binding Path=AdminSettingsIsVisible}"  x:Name="TelephelyekViewModel" />
                <ContentControl Grid.Row="1" Grid.Column="2"  Margin="10" IsEnabled="{Binding Path=AdminSettingsIsVisible}"  x:Name="CsoportokViewModel"/>
                <ContentControl Grid.Row="2" Grid.Column="0"  Margin="10" IsEnabled="{Binding Path=AdminSettingsIsVisible}"  x:Name="FelhasznalokViewModel"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
