<UserControl x:Class="IktatogRPCClient.Views.CsoportokView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IktatogRPCClient.Views"
             mc:Ignorable="d" 
             SnapsToDevicePixels="True"            
             MinHeight="450" MinWidth="380" FontSize="18" >
    <Border BorderBrush="#FF036A9A" BorderThickness="3" Margin="0" Padding="0">
        <Grid Margin="0">
            <Grid x:Name="CsoportokIsVisible" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Vertical">
                    <TextBlock Foreground="White" Padding="5" Background="#FF036A9A" FontSize="24">Csoportok</TextBlock>
                    <TextBlock FontSize="20" Margin="5">Telephely</TextBlock>
                    <ComboBox Margin="5 0" x:Name="ValaszthatoTelephely" SelectedItem="{Binding Path = SelectedTelephely}" DisplayMemberPath="Name"></ComboBox>
                    <TextBlock HorizontalAlignment="Center" FontSize="20" Margin="5 0">Csoportok a telephelyen</TextBlock>
                </StackPanel>
                <ListBox Margin="5" Grid.Row="1" MinHeight="200" x:Name="TelephelyCsoportjai" SelectedItem="{Binding Path=SelectedCsoport, Mode=TwoWay}" DisplayMemberPath="Name" />
                <Grid Grid.Row="2" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="CreateCsoport" MinHeight="45" FontSize="16" Grid.Column="0" Background="Green" Foreground="White"   Margin="5 0 5 5">Hozzáadás</Button>
                    <Button x:Name="ModifyCsoport" MinHeight="45" FontSize="16" Grid.Column="1" Background="Green" Foreground="White"   Margin="0 0 5 5">Módosítás</Button>
                    <Button x:Name="RemoveCsoport" MinHeight="45" FontSize="16" Grid.Column="2" Background="#FFFB2323" Foreground="White"  Margin="0 0 5 5">Törlés</Button>
                </Grid>
            </Grid>
            <Grid Grid.Row="0" x:Name="CreationIsVisible">
                <ContentControl  x:Name="ActiveItem"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>
